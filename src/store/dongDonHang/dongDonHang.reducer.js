const inititalState = {
    dongDonHang: []
}
export const dongDonHangReducer = (state = inititalState , action = {}) => {
    const { type, payload } = action;
    switch (type)  {
        default: 
            return state; 
    }
 }