const inititalState = {
    donHang: []
}
export const donHangReducer = (state = inititalState , action = {}) => {
    const { type, payload } = action;
    switch (type)  {
        default: 
            return state;
    }
 }